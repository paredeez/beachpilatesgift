# Beach Pilates Melbourne - Gift Vouchers

A modern, stylish gift voucher purchasing page for Beach Pilates Melbourne.

## Getting Started

First, install dependencies:

```bash
npm install
```

Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new).

Simply push to GitHub and import your repository in Vercel. It will automatically detect Next.js and deploy.

